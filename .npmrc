# Usar registry público de npm solo para este proyecto (no afecta el corporativo global)
registry=https://registry.npmjs.org/
# Evitar error de peer dependency con Prisma y TypeScript 4.x
legacy-peer-deps=true
